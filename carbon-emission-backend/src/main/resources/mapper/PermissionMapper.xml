<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bjfu.carbon.mapper.PermissionMapper">

    <resultMap id="BaseResultMap" type="com.bjfu.carbon.domain.Permission">
        <id column="id" property="id"/>
        <result column="permission_code" property="permissionCode"/>
        <result column="permission_name" property="permissionName"/>
        <result column="permission_type" property="permissionType"/>
        <result column="parent_id" property="parentId"/>
        <result column="path" property="path"/>
        <result column="component" property="component"/>
        <result column="icon" property="icon"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="created_time" property="createdTime"/>
        <result column="modified_time" property="modifiedTime"/>
    </resultMap>

    <!-- 获取用户的所有权限编码 -->
    <select id="getUserPermissions" resultType="java.lang.String">
        SELECT DISTINCT p.permission_code
        FROM permission p
        INNER JOIN role_permission rp ON p.id = rp.permission_id
        INNER JOIN user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND p.status = 1
    </select>

    <!-- 获取角色的所有权限编码 -->
    <select id="getRolePermissions" resultType="java.lang.String">
        SELECT DISTINCT p.permission_code
        FROM permission p
        INNER JOIN role_permission rp ON p.id = rp.permission_id
        WHERE rp.role_id = #{roleId}
          AND p.status = 1
    </select>

    <!-- 获取角色的所有权限ID -->
    <select id="getRolePermissionIds" resultType="java.lang.Long">
        SELECT DISTINCT p.id
        FROM permission p
        INNER JOIN role_permission rp ON p.id = rp.permission_id
        WHERE rp.role_id = #{roleId}
          AND p.status = 1
    </select>

    <!-- 获取权限树（所有权限） -->
    <select id="getPermissionTree" resultMap="BaseResultMap">
        SELECT *
        FROM permission
        WHERE status = 1
        ORDER BY sort_order ASC, id ASC
    </select>

    <!-- 根据父ID获取子权限列表 -->
    <select id="getPermissionsByParentId" resultMap="BaseResultMap">
        SELECT *
        FROM permission
        WHERE parent_id = #{parentId}
          AND status = 1
        ORDER BY sort_order ASC, id ASC
    </select>

</mapper>

